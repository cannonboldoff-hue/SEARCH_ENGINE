"use client";

/**
 * Re-exports the CSS-only hero as Hero3DScene.
 * The original 3D WebGL component was removed; this keeps imports working.
 */
export { HeroScene as Hero3DScene } from "../hero-scene";
